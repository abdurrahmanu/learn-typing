<template>
    <!-- <div ref="effectEl" v-show="isVisible" class="transition-all duration-100 bg-blue-500 rounded-full fixed w-2 h-2"></div> -->
</template>

<script setup>
// import { onMounted, ref } from 'vue';
// const effectEl = ref(null)
// const isVisible = ref(false)

// onMounted(() => {
//     window.addEventListener('click', event => {
//         isVisible.value ? isVisible.value = false : null
//         isVisible.value = true
//         effectEl.value.style.top = event.clientY + 'px'
//         effectEl.value.style.left = event.clientX + 'px'
//         setTimeout(() => isVisible.value = false, 1000 );
    
//     })
// })
</script>

